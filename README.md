# Human-Activity-Recognition
A PyTorch deep learning pipeline for Human Activity Recognition (HAR) using smartphone sensor data. This project explores the transition from a baseline FFNN trained on static, engineered features to a temporal 1D CNN trained on raw sequence data to solve complex physical overlaps like Sitting vs. Standing.
